\item \points{3e}

Render a video of an episode generated by the pre-trained policy, and a video of an episode generated by the policy tuned by DPO.

\begin{lstlisting}
$ python render.py --dpo --checkpoint ./results_dpo/[MODEL]/model.pt
\end{lstlisting}

How do they compare?

🐍
import re
with open('submission.tex') as f: print((re.search(r'% <SCPD_SUBMISSION_TAG>_3e(.*?)% <SCPD_SUBMISSION_TAG>_3e', f.read(), re.DOTALL)).group(1))
🐍